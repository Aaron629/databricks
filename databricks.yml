bundle:
  name: bundle_init
  uuid: 8751e876-6169-48ab-ab0a-eda238699c2a

include:
  - resources/*.yml

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  main:
    mode: development
    default: true
    workspace:
      host: https://dbc-e6d3d330-8898.cloud.databricks.com
      root_path: /Workspace/Users/aaronhsu629@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: work_order
      schema: ${workspace.current_user.short_name}

  uat:
    mode: production
    workspace:
      host: https://dbc-e6d3d330-8898.cloud.databricks.com
      root_path: /Workspace/Users/aaronhsu629@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: work_order
      schema: uat
    permissions:
      - user_name: aaronhsu629@gmail.com
        level: CAN_MANAGE

  prod:
    mode: production
    workspace:
      host: https://dbc-e6d3d330-8898.cloud.databricks.com
      root_path: /Workspace/Users/aaronhsu629@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: work_order
      schema: prod
    permissions:
      - user_name: aaronhsu629@gmail.com
        level: CAN_MANAGE